version: 2.1
orbs:
  docker: circleci/docker@0.5.0
jobs:
#  shellcheck:
#    - docker:
#      image: koalaman/shellcheck-alpine:stable
#    - steps:
#      - checkout
#      - run:
#          name: Check Scripts
#          command: |
#            find . -type f -name '*.sh' | wc -l
#            find . -type f -name '*.sh' | xargs shellcheck --severity=error --external-sources
  check-and-build-only:
#    working_directory:
    docker:
      - image: circleci/buildpack-deps:stretch
    steps:
      - checkout:
          path: ~/repo
      - setup_remote_docker:
          docker_layer_caching: true
#      - docker/check
      - docker/build:
          dockerfile: ~/repo/images/base-image/Dockerfile
          path: ~/repo/images/base-image
          image: brianmlima/apibuilder-demo-base

workflows:
  version: 2
  workflow:
    jobs:
#      - shellcheck
     - check-and-build-only:
        context: Dev-Tag-Service
